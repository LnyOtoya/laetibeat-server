[package]
name = "music_backend_source"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["fs", "sync"] }
serde = { version = "1.0", features = ["derive"] }
lofty = "0.18"
thiserror = "1.0"
