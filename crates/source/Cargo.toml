[package]
name = "music_backend_source"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["fs", "sync", "io-util"] }
serde = { version = "1.0", features = ["derive"] }
lofty = "0.18"
thiserror = "1.0"
async-trait = "0.1"
anyhow = "1.0"